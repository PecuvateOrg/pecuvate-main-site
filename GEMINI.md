# Pecuvate Main Site Agent Instructions

Read `CLAUDE.md` first. It is the canonical MWP Layer 0 file for this project and contains routing, project-wide rules, deployment metadata, and token-management guidance.

Then read `CONTEXT.md` when relevant to the task.

This repo is **public**. Its `memory.md` and `DEVLOG.md` are not kept here — they live in
the private Pecuvate hub at `../workspace-docs/pecuvate-main-site/`. Read and write them
there. Never recreate either file in this repo; both are gitignored and would be silently
lost.

These instructions are agent-generic unless a section is explicitly marked Claude-only.
